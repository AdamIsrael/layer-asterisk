includes: ['layer:basic']  # if you use any interfaces, add them here
options:
    basic:
        packages:
            - asterisk
            - asterisk-modules
            - asterisk-voicemail
            - python3-configobj
